import { useMemo, useState } from 'react'
import { useLocation, useNavigate } from 'react-router-dom'
import { AnimatedMetric } from './components/AnimatedMetric.jsx'
import { HumanNetwork } from './components/HumanNetwork.jsx'
import Navbar from './components/Navbar.jsx'
import Footer from './components/Footer.jsx'
import HomeHeroSection from './components/Hero.jsx'
import HomePage from './pages/Home.jsx'
import AboutPage from './pages/About.jsx'
import ServicesPage from './pages/Services.jsx'
import CareersPage from './pages/Careers.jsx'
import InsightsPage from './pages/Insights.jsx'
import ContactPage from './pages/Contact.jsx'

const services = [['Talent Acquisition', 'Find the right people for opportunities that matter.'], ['Executive Search', "Identify leaders capable of shaping what's next."], ['HR Consulting', 'Turn people challenges into practical business strategies.'], ['Employee Development', 'Build capabilities that grow with your organization.'], ['HR Operations', 'Make everyday people operations simpler and smarter.'], ['Workplace Strategy', 'Create environments where people and performance can thrive.']]
const jobs = [['Frontend Developer', 'Technology', 'Lahore', 'Full Time'], ['Talent Acquisition Specialist', 'People & Talent', 'Lahore', 'Full Time'], ['HR Business Partner', 'People Strategy', 'Islamabad', 'Full Time'], ['People Analytics Associate', 'Analytics', 'Remote', 'Full Time']]
const articles = [['RECRUITMENT', 'Why skills are becoming more important than job titles'], ['LEADERSHIP', 'What makes a high-performing team stay together'], ['CULTURE', "Building a workplace people don't want to leave"], ['CAREER', 'The skills shaping the next generation of work'], ['WORKPLACE', 'Why employee experience is now a business strategy']]
const navItems = ['Home', 'About', 'Services', 'Careers', 'Insights', 'Contact']

function Network() { return <HumanNetwork /> }
function Hero({ eyebrow, title, text }) { return <section className="page-hero"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{text && <p className="hero-summary">{text}</p>}</section> }
function CTA({ go, title = 'Ready to connect human potential with business impact?', button = 'Start a Conversation →' }) { return <section className="cta-section"><p className="eyebrow">PEOPLE. POTENTIAL. PROGRESS.</p><h2>{title}</h2><button className="primary-button" onClick={() => go?.('contact')}>{button}</button></section> }
function HomeFoundation() { return <HomeHeroSection /> }
function AboutAdditions() { return <><section className="light-section"><p className="eyebrow">OUR VALUES</p><h2>How we choose<br /><em>to show up.</em></h2><div className="values-grid">{[['01', 'Human'], ['02', 'Curious'], ['03', 'Responsible'], ['04', 'Bold']].map(([number, value]) => <article key={value}><b>{number}</b><h3>{value}</h3><p>We bring this value to every people decision.</p></article>)}</div></section><section className="dark-section"><p className="eyebrow">THE TEAM / CONCEPT PROFILES</p><h2>People behind<br /><em>the perspective.</em></h2><div className="team-grid">{['Founder & CEO', 'People Strategy Director', 'Talent Director', 'People Analytics Lead', 'Workplace Consultant'].map((role, i) => <article key={role}><span>0{i + 1}</span><h3>{role}</h3><p>NEXORA concept profile</p></article>)}</div></section></> }
function Metrics() { return <section className="index-section"><div><p className="eyebrow">THE PEOPLE INDEX</p><h2>A better workplace<br />can be <em>measured.</em></h2><p>Illustrative NEXORA demo metrics for this concept project.</p></div><div className="metrics">{[['84%', 'Talent Retention'], ['91%', 'Employee Growth'], ['87%', 'Culture Health'], ['72%', 'Leadership Readiness']].map(([value, label]) => <AnimatedMetric value={value} label={label} key={label} />)}</div></section> }
function Home({ go }) { return <><section className="home-hero"><div><p className="eyebrow">HUMAN RESOURCES × TECHNOLOGY</p><h1>Human potential,<br /><em>intelligently connected.</em></h1><p className="hero-summary">We help ambitious organizations find, develop, and retain the people who move their business forward.</p><div className="button-row"><button className="primary-button" onClick={() => go('contact')}>Start a Conversation →</button><button className="secondary-button" onClick={() => go('services')}>Explore Services</button></div></div><Network /></section><section className="trust-band"><p className="eyebrow">BUILT AROUND PEOPLE / DESIGNED FOR PROGRESS</p><h2>Better organizations begin<br />with <em>better connections.</em></h2><div className="trust-marks"><span>ORBITAL</span><span>MONUMENT</span><span>VANTA</span><span>ARC / FIELD</span></div></section><section className="light-section"><div className="section-heading"><div><p className="eyebrow">THE HUMAN POTENTIAL LOOP</p><h2>People are not resources.<br />They're <em>potential.</em></h2></div><p className="section-note">The right conditions help people do their best work, and help organizations move with confidence.</p></div><div className="feature-grid">{[['Discover', 'Identify the people and capabilities your organization needs.'], ['Develop', 'Turn individual strengths into measurable professional growth.'], ['Retain', 'Build environments where high-performing people want to stay.']].map(([title, text], i) => <article className="feature-card" key={title}><b>0{i + 1}</b><h3>{title}</h3><p>{text}</p><span>↗</span></article>)}</div></section><section className="process-section"><p className="eyebrow">TALENT → IMPACT</p><h2>Move people<br /><em>forward.</em></h2><div className="process-line">{['Talent', 'Connection', 'Development', 'Performance', 'Impact'].map((item, i) => <div key={item}><b>0{i + 1}</b><strong>{item}</strong></div>)}</div></section><section className="dark-section"><div className="section-heading"><div><p className="eyebrow">WHAT WE DO</p><h2>From hiring to<br /><em>human performance.</em></h2></div><button className="secondary-button" onClick={() => go('services')}>View all services →</button></div><div className="service-list">{services.map(([title, text], i) => <button className="service-row" key={title} onClick={() => go('services')}><b>0{i + 1}</b><strong>{title}</strong><span>{text}</span><i>↗</i></button>)}</div></section><Metrics /><section className="testimonial-section"><p className="eyebrow">SAMPLE / DEMO TESTIMONIALS</p><h2>Progress sounds<br /><em>different</em> for everyone.</h2><div className="testimonial-grid">{['NEXORA helped us connect our hiring strategy to the people we needed to grow.', 'The work made our culture feel less like a statement and more like a daily practice.', 'A sharper view of potential changed how our leaders plan for what comes next.'].map((quote, i) => <blockquote key={quote}><p>“{quote}”</p><footer>Demo partner 0{i + 1}<br /><small>Illustrative project content</small></footer></blockquote>)}</div></section><CTA go={go} /></> }

function About() { return <><Hero eyebrow="ABOUT NEXORA" title={<>We build better organizations by understanding the people inside them.</>} text="NEXORA connects recruitment, development and business strategy so human potential can become meaningful progress." /><section className="light-section story-grid"><div><p className="eyebrow">OUR STORY</p><h2>Problem.<br />Approach.<br /><em>Possibility.</em></h2></div><p>Traditional HR often separates recruitment, development and business strategy. We connect them with human understanding, useful data and practical technology.</p></section><section className="dark-section mission-grid"><div><p className="eyebrow">MISSION</p><h2>Make potential easier to <em>connect.</em></h2></div><div><p className="eyebrow">VISION</p><h2>A world where human potential creates <em>progress.</em></h2></div></section><section className="light-section"><p className="eyebrow">OUR PHILOSOPHY</p><h2>Useful beliefs.<br /><em>Human outcomes.</em></h2><div className="principle-grid">{[['People First', 'Technology should support people, not replace human understanding.'], ['Evidence Matters', 'Better HR decisions come from meaningful data and insight.'], ['Progress Over Perfection', 'Organizations should continuously learn and improve.']].map(([title, text]) => <article key={title}><span>+</span><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="dark-section"><p className="eyebrow">BRAND CONCEPT TIMELINE</p><h2>Always moving<br /><em>forward.</em></h2><div className="timeline">{[['2021', 'NEXORA concept established'], ['2022', 'Talent solutions launched'], ['2023', 'People development expanded'], ['2024', 'HR intelligence platform introduced'], ['2025', 'Connected workforce strategy']].map(([year, text]) => <div key={year}><b>{year}</b><span>{text}</span></div>)}</div></section><CTA /></> }

function Services({ go }) { const [active, setActive] = useState(0); return <><Hero eyebrow="SERVICES" title={<>Human strategy for organizations ready to <em>move forward.</em></>} text="A connected set of services for the moments when people and business need to move together." /><section className="dark-section service-explorer"><p className="eyebrow">THE SERVICE EXPLORER</p><h2>Choose the next<br /><em>right move.</em></h2><div className="explorer-layout"><div>{services.map(([title], i) => <button className={active === i ? 'explorer-row active' : 'explorer-row'} key={title} onClick={() => setActive(i)}><b>0{i + 1}</b><span>{title}</span><i>↗</i></button>)}</div><article className="explorer-detail"><p className="eyebrow">0{active + 1} / SERVICE</p><h3>{services[active][0]}</h3><p>{services[active][1]}</p><div className="detail-tags"><span>Strategy</span><span>People</span><span>Progress</span></div></article></div></section><CTA go={go} title="Your people strategy shouldn't stand still." button="Let's Build It →" /></> }

function Careers() { const [filters, setFilters] = useState({ search: '', department: 'All', location: 'All' }); const [selected, setSelected] = useState(null); const filtered = useMemo(() => jobs.filter((job) => { const text = job.join(' ').toLowerCase(); return text.includes(filters.search.toLowerCase()) && (filters.department === 'All' || job[1] === filters.department) && (filters.location === 'All' || job[2] === filters.location) }), [filters]); return <><Hero eyebrow="CAREERS / FIND YOUR NEXT OPPORTUNITY" title={<>Find work that<br /><em>moves you forward.</em></>} text="Explore opportunities where your skills and ambitions can create meaningful impact." /><section className="light-section careers-section"><p className="eyebrow">CAREER EXPLORER</p><h2>Find where<br /><em>you belong.</em></h2><div className="job-filters"><label>Search jobs<input placeholder="Search jobs..." value={filters.search} onChange={(e) => setFilters({ ...filters, search: e.target.value })} /></label><label>Department<select value={filters.department} onChange={(e) => setFilters({ ...filters, department: e.target.value })}>{['All', 'Technology', 'People & Talent', 'People Strategy', 'Analytics'].map((x) => <option key={x}>{x}</option>)}</select></label><label>Location<select value={filters.location} onChange={(e) => setFilters({ ...filters, location: e.target.value })}>{['All', 'Lahore', 'Islamabad', 'Remote'].map((x) => <option key={x}>{x}</option>)}</select></label></div><div className="jobs-grid">{filtered.map(([title, department, location, type]) => <article className="job-card" key={title}><p className="eyebrow">{department}</p><h3>{title}</h3><span>{location} · {type}</span><p>Build meaningful work with people who care about what comes next.</p><button className="text-button" onClick={() => setSelected({ title, department, location, type })}>View Position →</button></article>)}</div>{selected && <article className="job-detail"><button className="text-button" onClick={() => setSelected(null)}>Close ×</button><p className="eyebrow">{selected.department} · {selected.location} · {selected.type}</p><h3>{selected.title}</h3><p>ABOUT THE ROLE</p><p>Help NEXORA build thoughtful products and people experiences that connect potential with opportunity.</p><p>WHAT YOU'LL DO</p><p>Collaborate with a curious team, improve our systems, and turn complex challenges into clear progress.</p><button className="primary-button">Apply Now →</button></article>}</section><section className="dark-section benefits"><p className="eyebrow">WHY NEXORA</p><h2>Find work that<br /><em>grows with you.</em></h2><div className="benefit-row">{['Grow', 'Learn', 'Connect', 'Impact'].map((item, i) => <div key={item}><b>0{i + 1}</b><strong>{item}</strong></div>)}</div></section><CTA title="Don't see the right role yet?" button="Send Your Profile →" /></> }

function Insights() { const [category, setCategory] = useState('All'); const [subscribed, setSubscribed] = useState(false); const filtered = category === 'All' ? articles : articles.filter(([type]) => type === category); return <><Hero eyebrow="INSIGHTS / IDEAS THAT MOVE PEOPLE FORWARD" title={<>Ideas for the<br /><em>future of work.</em></>} text="Perspectives on people, leadership, careers and the organizations shaping tomorrow." /><section className="light-section insights"><article className="featured-insight"><p className="eyebrow">FEATURED INSIGHT / WORKPLACE</p><h2>The Human Advantage in an Automated Workplace</h2><span>Workplace · 8 min read</span><button className="text-button">Read Insight →</button></article><div className="insight-controls">{['All', 'RECRUITMENT', 'LEADERSHIP', 'WORKPLACE', 'CAREER', 'CULTURE'].map((item) => <button className={category === item ? 'filter active' : 'filter'} key={item} onClick={() => setCategory(item)}>{item}</button>)}</div><div className="article-grid">{filtered.map(([type, title], i) => <article className="article-card" key={title}><b>0{i + 1}</b><p className="eyebrow">{type}</p><h3>{title}</h3><p>Practical perspective for people building the future of work.</p><button className="text-button">Read article ↗</button></article>)}</div></section><Metrics /><section className="newsletter"><p className="eyebrow">NEXORA FIELD NOTES</p><h2>Stay connected to<br /><em>what's changing.</em></h2>{subscribed ? <p className="success-message">You're on the list. Thanks for staying curious.</p> : <form onSubmit={(e) => { e.preventDefault(); setSubscribed(true) }}><input type="email" required placeholder="Your email" aria-label="Your email" /><button className="primary-button">Subscribe →</button></form>}</section></> }

function Contact() { const [form, setForm] = useState({ name: '', email: '', service: 'Talent Acquisition', message: '' }); const [sent, setSent] = useState(false); const [error, setError] = useState(''); const submit = (e) => { e.preventDefault(); if (!form.name) return setError('Please enter your name.'); if (!form.email.includes('@')) return setError('Please enter a valid email address.'); if (!form.message) return setError('Please enter a message.'); setError(''); setSent(true) }; return <><Hero eyebrow="START A CONVERSATION" title={<>Tell us what<br /><em>you're building.</em></>} text="Whether you're growing a team, transforming your people strategy, or exploring your next opportunity, let's start with a conversation." /><section className="contact-section"><div><p className="eyebrow">LET'S TALK</p><h2>Good conversations<br />start with a <em>question.</em></h2><div className="contact-details"><strong>NEXORA</strong><span>Lahore / Pakistan</span><span>hello@nexora.co</span><span>+92 300 0000000</span></div></div>{sent ? <div className="success-panel"><p className="eyebrow">MESSAGE RECEIVED</p><h2>Thanks for reaching out.</h2><p>We'll be in touch soon.</p><button className="secondary-button" onClick={() => { setSent(false); setForm({ ...form, name: '', email: '', message: '' }) }}>Send another message</button></div> : <form className="contact-form" onSubmit={submit} noValidate><label>Full Name *<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Work Email *<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label>Company<input /></label><label>I'm interested in:<select value={form.service} onChange={(e) => setForm({ ...form, service: e.target.value })}>{services.map(([title]) => <option key={title}>{title}</option>)}<option>General Inquiry</option></select></label><label>Message *<textarea rows="5" value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} /></label>{error && <p className="form-error">{error}</p>}<button className="primary-button" type="submit">Send Message →</button></form>}</section></> }

function App() { const navigate = useNavigate(); const location = useLocation(); const page = location.pathname.replace('/', '') || 'home'; const go = (next) => { navigate(next === 'home' ? '/' : `/${next}`); window.scrollTo(0, 0) }; const Page = { home: HomePage, about: AboutPage, services: ServicesPage, careers: CareersPage, insights: InsightsPage, contact: ContactPage }[page] || HomePage; return <div className="site-shell"><Navbar /><main><Page go={go} /></main><Footer go={go} /></div> }
export default App
