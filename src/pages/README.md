# NEXORA pages

The six route surfaces are currently composed in `App.jsx` while the architecture is being stabilized. This folder is the extraction target for `Home`, `About`, `Services`, `Careers`, `Insights`, and `Contact` page modules.
